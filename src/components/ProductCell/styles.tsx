import {Platform, StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  content: {
    borderRadius: 1,
    borderWidth: 1,
    margin: 10,
    padding: 10,
  },
  title: {
    fontSize: 16,
    fontWeight: 'bold',
  },
  text: {
    fontSize: 14,
  },
});
